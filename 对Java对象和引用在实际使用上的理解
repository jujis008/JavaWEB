在观看教学视频及实际编写代码的过程中，我发现讲师的很多代码其实是多余的，比如从集合中取出一个元素（引用数据类型）；
对该元素进行修改操作后，将被修改的元素从集合中删除后重新添加到集合中，经过我实际验证发现其实这是多此一举的，这样会增加资源消耗，降低效率；
原因是，从集合中取出的元素其实是这个元素的引用，同理集合内部维护的也是这个元素的引用，它们都指向同一个实体对象；
对取出的引用操作和在集合内部对引用进行操作其本质上是一样的，它们操作是同一个对象，既然是同一个对象，那么在集合外对元素的修改也就是对集合内部元素的修改；
这两者是等同的，无需删除集合内元素再重新添加；
注意：如果是基本数据类型那两者就没有关系，不会相互影响

Ps:在编程中应该注意这个问题，虽然这样重复操作对性能的影响几乎可以忽略不计（删除引用再添加）
但是积少成多，并且省去这些操作也可以节约编码时间（如果没有因为考虑这个导致耽误时间的话）
